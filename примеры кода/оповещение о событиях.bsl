
// подробнее https://its.1c.ru/db/pubv8devui/content/230/hdoc
// оповещаем систему
СсылкаНаНовыйЭлемент = ДобавитьЭлементНаСервере ();
Оповестить("ОбновитьСписокТоваров");

// "ловим" событие и выполняем нужные действия
&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	Если ИмяСобытия = "ОбновитьСписокТоваров" Тогда
		Элементы.Список.Обновить();
			
	КонецЕсли;
	
КонецПроцедуры