// пример открытия формы программно с параметрами
ОткрытьФорму("ОбщаяФорма.ПутеводительПоСистеме", Новый Структура("РежимОткрытия", "Приветствие"));

// обработка параметров при открытии формы
Если Параметры.Свойство("РежимОткрытия") Тогда
	Возврат;
КонецЕсли;


